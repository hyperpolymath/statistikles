// SPDX-License-Identifier: CC-BY-SA-4.0
// Copyright (c) Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
= statistikles — Quick Start for Platform Maintainers
:toc:
:toclevels: 2

== Overview

This guide covers packaging, deploying, and maintaining statistikles for
distribution on your platform.

== Runtime Dependencies

* Julia 1.10+ (package dependencies are resolved by `Pkg` from `Project.toml`:
  CSV, DataFrames, Distributions, HTTP, JSON3, StatsBase)
* Optional, for the conversational interface only: an OpenAI-compatible LLM
  endpoint (defaults to LM Studio at `http://localhost:1234/v1`; override with
  `STATISTIKLES_LM_URL`, `STATISTIKLES_API_KEY`, `STATISTIKLES_MODEL`)

== Build from Source

[source,bash]
----
git clone https://github.com/hyperpolymath/statistikles.git
cd statistikles
just build-release
----

Output: a precompiled Julia project environment (no standalone binary — the
package is loaded with `julia --project=. -e 'using Statistikles'`)

== Packaging

=== Guix

[source,bash]
----
guix build -f guix.scm
----

=== Nix

[source,bash]
----
nix build
----

=== Container (Stapeln)

[source,bash]
----
just stapeln-export    # Generates Containerfile
podman build -t statistikles .
----

=== Manual Package

[source,bash]
----
just install --prefix=/usr/local
----

Files installed:

[cols="1,2"]
|===
| Path | Contents

| `$PREFIX/bin/`
| Executables

| `$PREFIX/share/statistikles/`
| Data files, assets

| `$PREFIX/share/doc/statistikles/`
| Documentation

| `$PREFIX/share/applications/`
| .desktop file (Linux, if GUI)

| `$PREFIX/share/man/man1/`
| Man pages
|===

== Configuration

Default config location: `$XDG_CONFIG_HOME/statistikles/config.toml`

Fallback: `$HOME/.config/statistikles/config.toml`

== Health Checks

[source,bash]
----
just doctor            # Full diagnostic
just run --version   # Version check
just run --selftest  # Built-in self-test
----

== Updating

[source,bash]
----
git pull
just build-release
just install --prefix=/usr/local
----

Or via OPSM: `opsm update statistikles`

== Security Notes

* License: MPL-2.0 (Palimpsest License)
* All dependencies SHA-pinned
* `panic-attacker` scan results: link:INSTALL-SECURITY-REPORT.adoc[]
* OpenSSF Scorecard: see badge in README

== Multi-Instance Deployment

For deploying multiple instances (e.g., different users or tenants):

[source,bash]
----
just install --prefix=/opt/statistikles-instance1 --config=/etc/statistikles/instance1.toml
just install --prefix=/opt/statistikles-instance2 --config=/etc/statistikles/instance2.toml
----

Each instance has isolated config, data, and logs.

== Reporting Issues

* Upstream: https://github.com/hyperpolymath/statistikles/issues
* With diagnostic: `just help-me` (pre-fills context)
